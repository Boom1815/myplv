name = "myplv-web"
compatibility_date = "2024-11-01"

# Site statique servi directement par Cloudflare Workers (assets natifs) —
# pas de script Worker ici, uniquement les fichiers buildés par Vite.
[assets]
directory = "./dist"
not_found_handling = "single-page-application"
